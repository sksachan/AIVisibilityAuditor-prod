Place governed query portfolio JSON files here.

For the current pilot, `inputs/audit_context.json` can act as the source of truth. If a portfolio JSON is added, `01_build_audit_context.py` will use it first.
